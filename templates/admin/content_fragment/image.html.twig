<figure>
    <img src="{{ fragment.data.file.url }}" alt="{{ fragment.data.caption }}" />
    {% if fragment.data.caption %}
        <figcaption>{{ fragment.data.caption }}</figcaption>
    {% endif %}
</figure>
